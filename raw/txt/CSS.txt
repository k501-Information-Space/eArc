/* ============================================
   K501 CYBERPUNK GATEWAY DESIGN
   ============================================ */

:root {
  --cp-primary: #00ff41;
  --cp-secondary: #ff006e;
  --cp-accent: #00d9ff;
  --cp-dark: #0a0e27;
  --cp-darker: #050812;
  --cp-border: #1a1f3a;
  --cp-text: #e0e6ff;
  --cp-text-dim: #8892b0;
  --cp-glow: 0 0 20px rgba(0, 255, 65, 0.3);
}

* {
  box-sizing: border-box;
}

body.cyberpunk-mode {
  background: linear-gradient(135deg, var(--cp-darker) 0%, #0f1535 100%);
  color: var(--cp-text);
  font-family: 'Courier New', monospace;
  line-height: 1.6;
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* ============================================
   HEADER
   ============================================ */

.gateway-header {
  background: linear-gradient(90deg, rgba(10, 14, 39, 0.95) 0%, rgba(15, 21, 53, 0.95) 100%);
  border-bottom: 2px solid var(--cp-primary);
  box-shadow: 0 0 30px rgba(0, 255, 65, 0.15);
  position: sticky;
  top: 0;
  z-index: 100;
}

.header-inner {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 24px;
  gap: 40px;
}

.logo-group {
  display: flex;
  align-items: center;
  gap: 16px;
}

.logo {
  font-weight: 700;
  font-size: 24px;
  color: var(--cp-primary);
  text-shadow: var(--cp-glow);
  letter-spacing: 2px;
}

.orcid-badge {
  background: rgba(0, 217, 255, 0.1);
  border: 1px solid var(--cp-accent);
  border-radius: 4px;
  padding: 6px 12px;
  font-size: 12px;
  color: var(--cp-accent);
}

.orcid-badge a {
  color: var(--cp-accent);
  text-decoration: none;
  transition: all 0.3s ease;
}

.orcid-badge a:hover {
  text-shadow: 0 0 10px var(--cp-accent);
}

.gateway-nav {
  display: flex;
  gap: 24px;
}

.nav-link {
  color: var(--cp-text);
  text-decoration: none;
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  position: relative;
}

.nav-link:hover {
  color: var(--cp-primary);
  text-shadow: var(--cp-glow);
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--cp-primary);
  transition: width 0.3s ease;
}

.nav-link:hover::after {
  width: 100%;
}

.header-line {
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--cp-primary), transparent);
}

/* ============================================
   MAIN LAYOUT
   ============================================ */

.gateway-main {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 520px;
  gap: 24px;
  padding: 32px 24px;
}

/* ============================================
   PANELS
   ============================================ */

.panel {
  background: linear-gradient(135deg, rgba(26, 31, 58, 0.8) 0%, rgba(15, 21, 53, 0.6) 100%);
  border: 1px solid var(--cp-border);
  border-radius: 8px;
  padding: 24px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(0, 255, 65, 0.1);
  transition: all 0.3s ease;
}

.panel:hover {
  border-color: var(--cp-accent);
  box-shadow: 0 8px 32px rgba(0, 217, 255, 0.2), inset 0 1px 0 rgba(0, 255, 65, 0.2);
}

.panel-primary {
  grid-column: 1;
}

.panel-bootframe {
  grid-column: 2;
  grid-row: 1 / 3;
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--cp-border);
}

.panel-header h2,
.panel-header h3 {
  margin: 0;
  color: var(--cp-primary);
  font-size: 20px;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.version-tag,
.status-indicator {
  background: rgba(255, 0, 110, 0.2);
  border: 1px solid var(--cp-secondary);
  color: var(--cp-secondary);
  padding: 4px 12px;
  border-radius: 4px;
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
}

.status-indicator {
  background: rgba(0, 255, 65, 0.2);
  border-color: var(--cp-primary);
  color: var(--cp-primary);
}

/* ============================================
   META & CONTENT
   ============================================ */

.meta-info {
  background: rgba(0, 217, 255, 0.05);
  border-left: 3px solid var(--cp-accent);
  padding: 12px 16px;
  margin-bottom: 20px;
  border-radius: 4px;
  font-size: 13px;
}

.meta-block {
  display: grid;
  grid-template-columns: 120px 1fr;
  gap: 16px 12px;
  margin: 0;
  padding: 0;
}

.meta-block dt {
  font-weight: 700;
  color: var(--cp-primary);
  text-transform: uppercase;
  font-size: 12px;
  letter-spacing: 1px;
}

.meta-block dd {
  margin: 0;
  color: var(--cp-text);
}

.meta-block a {
  color: var(--cp-accent);
  text-decoration: none;
  transition: all 0.3s ease;
}

.meta-block a:hover {
  text-shadow: 0 0 10px var(--cp-accent);
}

/* ============================================
   SUBSECTIONS
   ============================================ */

.subsection {
  margin-top: 28px;
  padding-top: 20px;
  border-top: 1px solid var(--cp-border);
}

.subsection h3 {
  color: var(--cp-accent);
  font-size: 16px;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin: 0 0 12px 0;
}

.feature-list {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-top: 12px;
}

.feature-tag {
  background: rgba(0, 255, 65, 0.15);
  border: 1px solid var(--cp-primary);
  color: var(--cp-primary);
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 12px;
  font-weight: 600;
}

.doc-list {
  list-style: none;
  margin: 12px 0;
  padding: 0;
}

.doc-list li {
  padding: 8px 0;
  border-bottom: 1px solid var(--cp-border);
}

.doc-list li:last-child {
  border-bottom: none;
}

.doc-list a {
  color: var(--cp-accent);
  text-decoration: none;
  transition: all 0.3s ease;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

.doc-list a:hover {
  color: var(--cp-primary);
  text-shadow: var(--cp-glow);
}

/* ============================================
   IFRAME WRAPPER
   ============================================ */

.iframe-wrapper {
  position: relative;
  margin: 16px 0;
  border-radius: 6px;
  overflow: hidden;
  box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.5);
}

iframe {
  width: 100%;
  height: 600px;
  border: 1px solid var(--cp-border);
  display: block;
  background: var(--cp-darker);
}

.bootframe-note {
  margin-top: 12px;
  font-size: 12px;
  color: var(--cp-text-dim);
  background: rgba(0, 217, 255, 0.05);
  padding: 8px 12px;
  border-radius: 4px;
  border-left: 2px solid var(--cp-accent);
}

.bootframe-note code {
  background: rgba(0, 0, 0, 0.3);
  color: var(--cp-primary);
  padding: 2px 6px;
  border-radius: 3px;
  font-family: 'Courier New', monospace;
}

/* ============================================
   FOOTER
   ============================================ */

.gateway-footer {
  background: linear-gradient(90deg, rgba(10, 14, 39, 0.95) 0%, rgba(15, 21, 53, 0.95) 100%);
  border-top: 1px solid var(--cp-border);
  padding: 20px 24px;
  text-align: center;
  color: var(--cp-text-dim);
  font-size: 12px;
}

.footer-content {
  max-width: 1400px;
  margin: 0 auto;
}

.footer-content p {
  margin: 4px 0;
}

.footer-content a {
  color: var(--cp-accent);
  text-decoration: none;
}

.footer-content a:hover {
  text-shadow: 0 0 10px var(--cp-accent);
}

.marker {
  font-family: 'Courier New', monospace;
  opacity: 0.7;
}

.footer-line {
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--cp-primary), transparent);
  margin-top: 12px;
}

/* ============================================
   RESPONSIVE
   ============================================ */

@media (max-width: 1200px) {
  .gateway-main {
    grid-template-columns: 1fr;
  }

  .panel-bootframe {
    grid-column: 1;
    grid-row: auto;
  }

  iframe {
    height: 500px;
  }
}

@media (max-width: 768px) {
  .header-inner {
    flex-direction: column;
    gap: 16px;
    padding: 16px;
  }

  .gateway-nav {
    gap: 12px;
  }

  .nav-link {
    font-size: 12px;
  }

  .gateway-main {
    padding: 16px;
    gap: 16px;
  }

  .panel {
    padding: 16px;
  }

  .panel-header h2,
  .panel-header h3 {
    font-size: 16px;
  }

  .meta-block {
    grid-template-columns: 100px 1fr;
  }

  iframe {
    height: 400px;
  }
}

@media (max-width: 480px) {
  .logo {
    font-size: 18px;
  }

  .orcid-badge {
    font-size: 10px;
    padding: 4px 8px;
  }

  .gateway-main {
    padding: 12px;
  }

  .panel {
    padding: 12px;
  }

  .subsection {
    margin-top: 16px;
  }

  .subsection h3 {
    font-size: 14px;
  }

  iframe {
    height: 300px;
  }

  .feature-list {
    gap: 4px;
  }

  .feature-tag {
    font-size: 10px;
    padding: 3px 8px;
  }
}
