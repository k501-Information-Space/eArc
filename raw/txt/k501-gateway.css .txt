:root{
  --bg0:#050812;
  --bg1:#0f1535;
  --panel: rgba(26,31,58,.55);
  --border:#1a1f3a;
  --text:#e0e6ff;
  --dim:#8892b0;

  --p:#00ff41;
  --a:#00d9ff;

  --radius: 10px;
}

*{ box-sizing:border-box; }

body.k501{
  margin:0;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Courier New", monospace;
  background: linear-gradient(135deg, var(--bg0), var(--bg1));
  color: var(--text);
  line-height: 1.5;
}

.header{
  position: sticky;
  top: 0;
  z-index: 10;
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding: 16px 18px;
  background: rgba(10,14,39,.92);
  border-bottom: 1px solid var(--border);
}

.brand{ display:flex; gap:12px; align-items:baseline; }
.logo{
  font-weight: 800;
  letter-spacing: 2px;
  color: var(--p);
}
.subtitle{ color: var(--dim); font-size: 12px; }

.badge{
  display:inline-block;
  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid var(--border);
  color: var(--dim);
  font-size: 12px;
}

.badge.ok{ border-color: rgba(0,255,65,.35); color: var(--p); }
.badge.err{ border-color: rgba(255,0,110,.35); color: #ff006e; }
.badge.busy{ border-color: rgba(0,217,255,.35); color: var(--a); }

.layout{
  max-width: 1100px;
  margin: 0 auto;
  padding: 18px;
  display:grid;
  gap: 14px;
}

.panel{
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px;
}

.panel h2{
  margin: 0 0 10px 0;
  font-size: 14px;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--p);
}

.counters{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}

.counter{
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 12px;
  background: rgba(0,0,0,.18);
}

.label{ color: var(--dim); font-size: 12px; }
.value{ font-size: 20px; color: var(--text); margin-top: 4px; }

.meta{
  display:flex;
  gap: 18px;
  margin-top: 10px;
  color: var(--dim);
  font-size: 12px;
  flex-wrap: wrap;
}

.links{ display:flex; gap:10px; margin-top: 12px; flex-wrap:wrap; }
.link{
  color: var(--a);
  text-decoration:none;
  border-bottom: 1px dashed rgba(0,217,255,.35);
  padding-bottom: 1px;
}
.link:hover{ color: var(--p); border-bottom-color: rgba(0,255,65,.35); }

.feed{
  display:flex;
  flex-direction:column;
  gap: 8px;
}

.frame{
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 10px 12px;
  background: rgba(0,0,0,.14);
  font-size: 12px;
}

.frame .k{ color: var(--dim); }
.frame .v{ color: var(--text); }

.btn{
  border: 1px solid rgba(0,217,255,.35);
  background: rgba(0,217,255,.10);
  color: var(--a);
  padding: 10px 12px;
  border-radius: var(--radius);
  cursor:pointer;
}
.btn:hover{ border-color: rgba(0,255,65,.35); color: var(--p); }

.hint{ margin-top: 10px; color: var(--dim); font-size: 12px; }

.footer{
  padding: 14px 18px;
  color: var(--dim);
  border-top: 1px solid var(--border);
  background: rgba(10,14,39,.65);
}
.dim{ opacity: .85; }

@media (max-width: 720px){
  .counters{ grid-template-columns: 1fr; }
}