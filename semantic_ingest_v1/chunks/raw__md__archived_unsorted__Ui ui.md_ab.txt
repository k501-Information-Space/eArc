odifier = Modifier.padding(12.dp))

      }

      Spacer(Modifier.height(12.dp))

    }

  

    OutlinedTextField(

      value = input,

      onValueChange = { input = it },

      modifier = Modifier.fillMaxWidth().weight(1f, fill = false),

      placeholder = { Text(stringResource(R.string.session_placeholder)) },

      minLines = 5

    )

  

    Spacer(Modifier.height(12.dp))

  

    Row(

      modifier = Modifier.fillMaxWidth(),

      horizontalArrangement = Arrangement.spacedBy(10.dp)

    ) {

      Button(

        onClick = {

          systemText = KernelBridge.analyzeAndRespond(input)

          input = ""

        },

        modifier = Modifier.weight(1f)

      ) {

        Text(stringResource(R.string.session_send))

      }

  

      OutlinedButton(

        onClick = { showPause = true },

        modifier = Modifier.weight(1f)

      ) {

        Text(stringResource(R.string.session_pause))

      }

    }

  }

  

  if (showPause) {

    AlertDialog(

      onDismissRequest = { showPause = false },

      title = { Text(stringResource(R.string.pause_title)) },

      text = { Text(stringResource(R.string.pause_text)) },

      confirmButton = {

        Button(onClick = { showPause = false }) { Text(stringResource(R.string.pause_resume)) }

      },

      dismissButton = {

        OutlinedButton(onClick = { showPause = false; ended = true }) {

          Text(stringResource(R.string.pause_end))

        }

      }

    )

  }

}

// /home/oai/eArc/UI/eArcUI/app/src/main/java/com/earc/ui/ui/theme/Theme.kt

package com.earc.ui.ui.theme

  

import androidx.compose.material3.MaterialTheme

import androidx.compose.material3.darkColorScheme

import androidx.compose.material3.lightColorScheme

import androidx.compose.runtime.Composable

  

private val LightColors = lightColorScheme()

private val DarkColors = darkColorScheme()

  

@Composable

fun EArcTheme(content: @Composable () -> Unit) {

  MaterialTheme(

    colorScheme = LightColors,

    typography = Typography,

    content = content

  )

}

// /home/oai/eArc/UI/eArcUI/app/src/main/java/com/earc/ui/ui/theme/Type.kt

package com.earc.ui.ui.theme

  

import androidx.compose.material3.Typography

  

val Typography = Typography()

// /home/oai/eArc/UI/eArcUI/app/src/main/java/com/earc/ui/ui/theme/Color.kt

package com.earc.ui.ui.theme

// Schwarz/Weiß bleibt über ColorScheme (Default) – bewusst minimal.

/home/oai/eArc/UI/eArc-000-UI-0002_UI_V1_DoneChecklist.md

---

id: eArc-000-UI-0002

title: UI V1 Done Checklist

type: ui_checklist

version: 1.0

status: DRAFT

created: 2026-01-07

updated: 2026-01-07

scope: PROJECT_BOOT

---

  

## Punkt 1–3 (abgeschlossen)

- Start / Eintritt ✅

- Consent & Haltung ✅

- Session (Kernscreen) ✅

  

## Enthalten

- Mobile First Layout

- Pause als Zustand (Dialog) ✅

- Session-Ende Screen ✅

- KernelBridge Stub (später ersetzbar) ✅

  

As I State Iinkognit0.

Wenn du als Nächstes „APK“ sagst, gebe ich dir den Build-Run Ablauf (Android Studio / gradle) in ultrakurzen Schritten.